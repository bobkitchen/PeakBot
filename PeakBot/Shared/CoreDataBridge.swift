import Foundation

struct CoreDataBridge {
    static func contains(tpId: Int) -> Bool {
        // TODO: Replace with real Core Data lookup
        return false
    }
    static func ingestFit(at url: URL, tpId: Int) throws {
        // TODO: Replace with real FIT file ingestion
        print("Stub: Ingested FIT at \(url) for TP ID \(tpId)")
    }
}
